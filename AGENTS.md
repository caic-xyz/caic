# AGENTS.md

Development guidelines for the caic project.

For specific guidelines, see:

- [CI/CD Guidelines](.github/AGENTS.md) - Read before editing GitHub Actions
- [Backend Guidelines](backend/AGENTS.md) - Read before editing Go code in backend/
- [Frontend Guidelines](frontend/AGENTS.md) - Read before editing frontend code

Run `make` for the current targets; each slow or environment-dependent target
documents its cost beside its definition.

## Project Overview

caic manages multiple coding agents in parallel. Each task runs in an isolated [md](https://github.com/caic-xyz/md) container with Claude Code communicating via streaming JSON over SSH.

### Frontend / Android Boundary

`frontend/` (SolidJS) owns caic screen-mode product UI. `android/gomode/` owns
the Android shell around backend-hosted frontends; see `android/AGENTS.md` for
the module split. User-facing parity applies to shared shell capabilities, not
to caic task-list/detail/diff/widget UI, which must stay in the hosted web
frontend.

### Documentation Changes

- **Do NOT** create a markdown file listing changes you made.
- **Always update the purpose comment** at the beginning of a source file when
  the code's purpose changes. This comment is used to generate the file index
  in `AGENTS.md`.

### Professionalism

**Mandatory**: after making changes run `make fix`, then `make verify`.
`make fix` applies every autofix and refreshes the generated file index and
architecture diagram; `make verify` is the read-only static gate and the
pre-push gate. Unit tests are `make test`. Run `make refresh-generated` after
changing API DTOs/routes, generated SDK inputs, file-indexed source comments,
or backend package layout.

### Git Hooks

`pnpm install` configures the versioned hooks in `scripts/hooks/`; run
`pnpm hooks:install` (or `make git-hooks`, which also configures the merge
driver) to restore them after an installation that skipped lifecycle scripts.
The hook scripts document their own rules.

### Performance

When changing startup, parsing, serialization, cache, compression, hot-loop, or
request-path behavior, run `make benchmark` (Go benchmarks plus `scripts/bench.ts`)
before and after the change, add a focused benchmark when the changed path has
none, and report the deltas. Benchmarks requiring local production data remain
documented beside their source and are not part of `make benchmark`.

### E2E Tests

`make test-e2e` runs the Playwright suite against the fake backend server; see
the header of `e2e/playwright.config.ts` for the constraints (frontend-build
sharing, targeted runs, screenshot gating). Android flows use
`make android-e2e`, which starts or reuses the emulator first.

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `.custom-gcl.yml`: golangci-lint custom binary build config: methodfilecheck module plugin.
- `.github/AGENTS.md`: CI/CD Workflows
- `.golangci.yml`: Static analysis linter rules and enabled checks for the Go codebase.
- `.goreleaser.yml`: Defines cross-platform build targets, archives, and release publishing for the CLI binary.
- `DEV.md`: Development
- `Makefile`: Build, verify, test, and development workflow targets for the full stack (Go backend, TypeScript frontend, Android).
- `README.md`: caic
- `android/AGENTS.md`: Android Project
- `android/gomode/AGENTS.md`: Go Mode Android App
- `apisdkgen/AGENTS.md`: API generation SDK for TypeScript, Kotlin and Go
- `backend/AGENTS.md`: Go Backend Server and Agents
- `contrib/README.md`: contrib
- `contrib/install.sh`: Install caic and md via: curl -fsSL https://caic.xyz/install.sh | bash
- `e2e/helpers.ts`: Shared e2e test helpers: typed API client and utilities.
- `e2e/playwright.config.ts`: End-to-end browser test configuration using a fake backend server.
- `e2e/tests/account-menu.spec.ts`: End-to-end tests for account-menu layout and navigation.
- `e2e/tests/diff-mobile.spec.ts`: End-to-end tests for repository-diff mobile layout and patch retry focus.
- `e2e/tests/error-handling.spec.ts`: Error handling and edge case tests.
- `e2e/tests/fork-mobile.spec.ts`: E2E tests for mobile fork-task navigation and layout.
- `e2e/tests/gen-screenshots.spec.ts`: Generate screenshots for the documentation site.
- `e2e/tests/image-attach.spec.ts`: E2E tests for image attachment: API image support and screenshot capture UI flow.
- `e2e/tests/keyboard-navigation.spec.ts`: End-to-end keyboard-only navigation through repository selection, task creation, and task focus.
- `e2e/tests/multi-turn.spec.ts`: Multi-turn interaction and concurrent task tests.
- `e2e/tests/native-subagents.spec.ts`: Tests canonical native activity rendering, keyboard disclosure, mobile containment, and restored SSE history.
- `e2e/tests/plan-and-ask.spec.ts`: E2E tests for ExitPlanMode "Clear and execute plan" button and AskUserQuestion card.
- `e2e/tests/prompt-input.spec.ts`: Screenshots of the prompt input with short and long text to verify button layout.
- `e2e/tests/quota-recovery.spec.ts`: E2E tests for guided quota recovery on desktop and mobile task paths.
- `e2e/tests/repos-api.spec.ts`: API-only tests for repos and harnesses endpoints.
- `e2e/tests/stats-mobile.spec.ts`: End-to-end coverage for task statistics navigation and mobile viewport containment.
- `e2e/tests/task-card-repo-state.spec.ts`: E2E tests task-card repository-state summaries for single and mapped repositories.
- `e2e/tests/task-lifecycle.spec.ts`: End-to-end tests for the task lifecycle using a fake backend.
- `e2e/tests/tasks-api.spec.ts`: API-only tests for task lifecycle and repository status endpoints (no browser UI).
- `e2e/tests/widget.spec.ts`: E2E tests for generative UI widget rendering.
- `e2e/visual.ts`: Deterministic browser setup and capture helpers for documentation screenshots.
- `eslint.config.js`: Linting rules and plugin configuration for TypeScript and SolidJS source files.
- `frontend/AGENTS.md`: SolidJS Web Frontend
- `gomode/AGENTS.md`: Go Mode Server Library
- `oauth/AGENTS.md`: OAuth 2.0 Package
- `pnpm-workspace.yaml`: pnpm configuration
- `scripts/analyze_trace.py`: Analyze a Go runtime/trace binary file for performance issues.
- `scripts/android_e2e.py`: Run Android E2E tests against the fake backend.
- `scripts/android_sdk.py`: Install Android SDK packages and set up the caic emulator.
- `scripts/android_start_emulator.py`: Reuse a connected Android device or start the caic_test emulator.
- `scripts/bench.ts`: Runs frontend benchmarks on tinybench (the engine vitest bench used) without vitest.
- `scripts/check-staged.sh`: Validates the staged snapshot with focused repository checks.
- `scripts/halo-emulator.py`: Run the Brilliant Labs Halo Lua emulator with uv-managed dependencies.
- `scripts/install-git-hooks.sh`: Configures this checkout to run the repository's versioned Git hooks.
- `scripts/lint_binaries.py`: Lint git-tracked files for unexpected binary data and executable files.
- `scripts/lint_build_tags.py`: Compile-check every Go build tag that guards code the default build skips.
- `scripts/lint_frontend_styles.mjs`: Lints frontend CSS tokens, modules, raw colors, and presentation embedded in TSX.
- `scripts/precompress_dist.py`: Precompress built assets so a static handler can serve them verbatim.
- `scripts/quiet-test-reporter.mjs`: Reports Node test failures and diagnostics, with compact progress only on CI.
- `scripts/run-concurrently.sh`: Runs independent commands concurrently, or sequentially in serial mode.
- `scripts/run-dev.py`: Run caic development server with a temporary config directory.
- `scripts/run_python_tests.py`: Run every Python unit-test script and propagate the first failure.
- `scripts/test_android_e2e.py`: Unit tests for Android E2E device discovery and fake fixture selection.
- `scripts/test_android_start_emulator.py`: Unit tests for Android emulator discovery used by the E2E launcher.
- `scripts/test_lint_build_tags.py`: Unit tests for Go build-tag discovery used by the compile-check lint.
- `scripts/test_run_python_tests.py`: Unit tests for fail-closed Python unit-test execution.
- `scripts/test_visual_screenshots.py`: Unit tests for deterministic screenshot luminance comparison and render modes.
- `scripts/update_agents_file_index.py`: AGENTS.md index generation, summary validation, CLAUDE.md symlinks.
- `scripts/update_backend_architecture.py`: Update backend architecture diagrams from production and unit-test imports.
- `scripts/visual_screenshots.py`: Render, check, or update deterministic frontend and Android documentation screenshots.
- `sdk/AGENTS.md`: SDKs
- `sdk/halo/AGENTS.md`: Halo SDK
- `stylelint.config.js`: Enforces consistent styles for Solid component CSS modules.
- `vite.config.ts`: Vite configuration for the SolidJS frontend: plugins, path aliases, and the
<!-- END FILE INDEX -->
