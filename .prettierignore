# Tool-owned locks, generated artifacts, and testdata.
node_modules
backend/frontend/dist
# Gradle writes each Android and Kotlin module's output under build/, and the
# .gitignore files that hide those directories are nested, where Prettier never looks.
build
coverage
pnpm-lock.yaml
sdk/*/API.md
backend/docs/ARCHITECTURE.md
sdk/*/ts/**/*.gen.ts
**/testdata/**
backend/internal/agent/*/testdata/*.md
# AGENTS.md indexes are written by scripts/update_agents_file_index.py.
AGENTS.md
CLAUDE.md
# Playwright and screenshot failure artifacts written by the e2e targets.
test-results
visual-screenshots-failures
