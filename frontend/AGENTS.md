# SolidJS Web Frontend

SolidJS web UI for caic. Vite builds into `backend/frontend/dist/`.

## Required Validation

Run `make build` after every frontend change. Keep regenerated tracked
`backend/frontend/dist/*.br` assets.

Use:

```bash
pnpm test:coverage
pnpm typecheck
```

## Testing

Prefer semantic queries for user-visible interactions. Use stable
`data-testid` contracts for internal component identities that should survive
copy and layout changes.

Query portal-rendered content through `screen`.

Components using router hooks require router context or a router mock. Prefer a
shared test helper over repeating router mocks.

## Styling and Theming

The frontend currently ships one light application theme. Design tokens provide
a stable semantic boundary for future themes; do not add component-level theme
branches prematurely.

### Token Ownership

- Put application-wide tokens in `src/global.css`.
- Put component-specific tokens in the owning CSS Module.
- Put document-specific tokens in the owning HTML document.
- Local tokens are visible only within their owning source file.
- `WidgetShell.html` is a separate document and cannot inherit application
  tokens from the parent page.

### Token Design

Name tokens by semantic role, not by their current color:

- Prefer `--color-danger`, `--color-bg-elevated`, and `--color-on-primary`.
- Avoid names such as `--color-red`, `--light-gray`, or value-based names.
- Pair filled surfaces with explicit foreground tokens where contrast matters.
- Keep semantically different roles separate even when they currently share a
  value.
- Use aliases only when two roles are intentionally coupled.
- Derive translucent variants from their base token with `color-mix()` instead
  of duplicating RGB channels.

### Surface Hierarchy

Use the existing surface families consistently:

- Application background: page canvas.
- Surface: inset cards and content regions.
- Elevated surface: menus, dialogs, and popups.
- Input surface: editable controls and their hover state.
- Inverse/media surface: dark controls, camera, and VNC content.

Do not use border tokens as backgrounds or foreground tokens on unrelated
surface families solely because their current values match.

### States and Interaction

Use explicit tokens for hover, focus, selected, disabled, success, warning, and
danger states. Preserve visual distinctions between task states unless a
product change intentionally combines them.

Focus indicators must remain visible and must not rely only on a color change.
Background/foreground pairs should maintain accessible contrast.

### Theme Changes

Implement a future application theme by overriding shared semantic tokens at a
top-level theme selector. Do not duplicate component rules for each theme.

Separate documents such as `WidgetShell.html` require their own token overrides
and explicit theme propagation.

### Enforcement

Direct color values are allowed only in CSS custom-property declarations.
Property values must consume variables with `var(...)`.

Use `var(--radius-*)` for standard border radii. Percentage radii and
component-specific geometry are allowed when they express shape rather than a
design-system radius.

Run after styling changes:

```bash
python3 scripts/lint_css_vars.py
make build
```

## Widget Sandbox

`WidgetCard.tsx` hosts the sandboxed iframe. `WidgetShell.html` owns the iframe
document, CSP, local styles, and runtime script.

Do not add `allow-same-origin` to an iframe that also permits scripts.

## Mobile

Do not add a back button to the mobile task-detail header. Browser and platform
navigation own that behavior.

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `public/sw.js`: Service worker for caic PWA: caches only immutable hashed assets.
- `src/App.module.css`: Styles for the app shell and its diagnostic error fallback.
- `src/App.test.tsx`: Tests for app-shell task creation, repo selection, and harness preferences.
- `src/App.tsx`: Application shell: top-level chrome, dialogs, diagnostic error boundary, Go Mode browser shell, and routed panes.
- `src/AppState.tsx`: Application state store: owns task data, settings, SSE wiring, and task actions.
- `src/AuthContext.tsx`: Auth context: tracks the current user and auth configuration.
- `src/TaskNumberMap.test.ts`: Tests caic task numbering for voice-mode task references.
- `src/TaskNumberMap.ts`: Bidirectional caic task ID ↔ active-first 1-based voice number mapping.
- `src/api.test.ts`: Tests task SSE API parsing for history errors and resume resets.
- `src/api.ts`: Singleton API client for the caic web UI.
- `src/components/AccountMenu.module.css`: Styles for the AccountMenu avatar button and dropdown.
- `src/components/AccountMenu.tsx`: Navbar account menu: avatar button with a dropdown for settings and sign-out.
- `src/components/AutoResizeTextarea.module.css`: Styles for the contenteditable AutoResizeTextarea component.
- `src/components/AutoResizeTextarea.test.tsx`: Tests for the AutoResizeTextarea component.
- `src/components/AutoResizeTextarea.tsx`: Auto-resizing contenteditable prompt with keyboard submission and caret restoration.
- `src/components/Button.module.css`: Styles for the reusable Button component variants, icons, and loading state.
- `src/components/Button.tsx`: Reusable button component with variant and icon support.
- `src/components/CIDot.module.css`: CI status dot with tooltip showing check details.
- `src/components/CIDot.tsx`: Shared CI status dot used in TaskCard and TaskList repo headers.
- `src/components/CameraCapture.module.css`: Styles for the CameraCapture dialog.
- `src/components/CameraCapture.tsx`: Camera capture dialog: opens webcam, lets user take a photo, returns base64 ImageData.
- `src/components/CloneRepoDialog.module.css`: Styles for the CloneRepoDialog modal.
- `src/components/CloneRepoDialog.tsx`: Modal dialog for cloning a git repository by URL.
- `src/components/DiffDetail.module.css`: Styles for the full-page DiffView component.
- `src/components/DiffDetail.test.ts`: Tests for DiffDetail diff parsing utilities.
- `src/components/DiffDetail.tsx`: Full-page diff viewer for a task's file changes.
- `src/components/Dropdown.test.tsx`: Tests for reusable dropdown keyboard dismissal and focus restoration.
- `src/components/Dropdown.tsx`: Reusable dropdown menu with arrow navigation, dismissal, and trigger focus restoration.
- `src/components/ForkDialog.module.css`: Styles for the ForkDialog modal.
- `src/components/ForkDialog.tsx`: Fork-task modal dialog: prompt, extra repos, harness/model/effort, runtime toggles.
- `src/components/FormControls.module.css`: Shared form control component styles for selects and capability toggle chips.
- `src/components/FormControls.tsx`: Shared form control components for task harness selects and capability toggles.
- `src/components/KeyboardShortcuts.module.css`: Styles for the keyboard-shortcuts reference dialog.
- `src/components/KeyboardShortcuts.tsx`: Global keyboard navigation and the discoverable keyboard-shortcuts reference dialog.
- `src/components/Layout.module.css`: Shared layout component styles for route panes and detail-pane slots.
- `src/components/Layout.tsx`: Shared route layout wrappers for split panes and detail-pane slots.
- `src/components/MainLayout.module.css`: Styles for the new-task form in the main layout.
- `src/components/MainLayout.tsx`: Default layout: task creation, sidebar/detail panes, and native voice task-number synchronization.
- `src/components/ModalDialog.module.css`: Shared visual foundation for native modal dialogs.
- `src/components/ModalDialog.test.tsx`: Tests for the shared native modal dialog's dismissal and Escape behavior.
- `src/components/ModalDialog.tsx`: Shared native modal dialog behavior: dismissal, Escape isolation, and visual elevation.
- `src/components/ProcessDetail.module.css`: Styles for the full-page ProcessDetail component with tree view.
- `src/components/ProcessDetail.test.tsx`: Tests for the process tree builder used by ProcessDetail.
- `src/components/ProcessDetail.tsx`: Full-page process tree viewer for a task's container.
- `src/components/ProgressPanel.module.css`: Styles for the ProgressPanel component.
- `src/components/ProgressPanel.tsx`: ProgressPanel renders active todos and subagent count from the agent event stream.
- `src/components/PromptInput.module.css`: Styles for the PromptInput component: container, drag-over, and image previews.
- `src/components/PromptInput.test.tsx`: Tests for the PromptInput component.
- `src/components/PromptInput.tsx`: Reusable prompt input with image support: paste, drag & drop, attach button, and preview strip.
- `src/components/RepoChipStrip.module.css`: Styles for the RepoChipStrip component.
- `src/components/RepoChipStrip.test.tsx`: Tests for RepoChipStrip repository selection and keyboard behavior.
- `src/components/RepoChipStrip.tsx`: Reusable repo chip strip with branch editing and repository management.
- `src/components/SearchableSelect.module.css`: Reusable searchable select (combobox) component styles.
- `src/components/SearchableSelect.test.tsx`: Tests for the SearchableSelect combobox: filtering, keyboard navigation,
- `src/components/SearchableSelect.tsx`: Reusable searchable select (combobox) with keyboard navigation and optional grouped options.
- `src/components/SettingsForm.module.css`: Styles for the SettingsPage full-page settings view.
- `src/components/SettingsForm.tsx`: SettingsForm renders the application settings controls.
- `src/components/StatsIcon.module.css`: Styles for the StatsIcon component: 2×2 bar chart icon, popup panel, and perf table.
- `src/components/StatsIcon.tsx`: StatsIcon renders a 2×2 bar-chart icon in the task header that opens a popup
- `src/components/TaskActionsMenu.module.css`: Shared task actions menu styles.
- `src/components/TaskActionsMenu.tsx`: Shared task actions menu used by task details and task-card context menus.
- `src/components/TaskCard.module.css`: Compact summary card for a single task.
- `src/components/TaskCard.test.tsx`: Tests for the compact task card summary.
- `src/components/TaskCard.tsx`: Compact card for a single task, used in the sidebar task list.
- `src/components/TaskDetail.module.css`: Styles for the TaskView component.
- `src/components/TaskDetail.test.tsx`: Tests for TaskDetail navigation, prompts, and SSE connection behaviour.
- `src/components/TaskDetail.tsx`: TaskDetail renders agent output, task context, and task actions.
- `src/components/TaskInfo.module.css`: Styles for the full-page TaskInfo component with launch config and runtime metadata.
- `src/components/TaskInfo.test.tsx`: Tests for TaskInfo runtime metadata rendering.
- `src/components/TaskInfo.tsx`: Full-page task metadata viewer for launch config and runtime facts.
- `src/components/TaskList.module.css`: Sidebar task list panel.
- `src/components/TaskList.tsx`: Sidebar task list with collapsible panel, grouped by repo for active tasks.
- `src/components/Toasts.module.css`: Styles for the Toasts notification container.
- `src/components/Toasts.tsx`: Toast notification stack rendered via a portal at the bottom of the viewport.
- `src/components/Tooltip.module.css`: Tooltip shown on hover (desktop) and tap (mobile).
- `src/components/Tooltip.tsx`: Tooltip: hover on desktop, tap-to-toggle on mobile.
- `src/components/UnifiedDiffBlock.module.css`: Styles for the shared UnifiedDiffBlock component.
- `src/components/UnifiedDiffBlock.test.tsx`: Tests for the shared UnifiedDiffBlock component.
- `src/components/UnifiedDiffBlock.tsx`: UnifiedDiffBlock renders annotated unified diffs with shared line coloring.
- `src/components/UsageBadges.module.css`: Usage badge styles — provider-grouped pills.
- `src/components/UsageBadges.test.tsx`: Tests for UsageBadges component.
- `src/components/UsageBadges.tsx`: Usage badges: per-provider grouped pills with color-coded thresholds.
- `src/components/VncViewer.module.css`: Styles for the VNC desktop viewer.
- `src/components/VncViewer.tsx`: noVNC-based desktop viewer for a task's container display.
- `src/components/WidgetCard.module.css`: WidgetCard: sandboxed iframe for agent-generated HTML widgets.
- `src/components/WidgetCard.tsx`: Sandboxed iframe widget card for agent-generated HTML widgets.
- `src/components/diffLines.ts`: Diff parsing and moved-block annotation for unified diffs.
- `src/errorReport.test.ts`: Tests for frontend error diagnostic report formatting.
- `src/errorReport.ts`: Frontend error diagnostics: formats copyable browser context for render-failure reports.
- `src/formatting.test.ts`: Tests for shared formatting utilities.
- `src/formatting.ts`: Shared formatting utilities for caic web UI values.
- `src/global.css`: Global resets and design tokens for the web frontend.
- `src/gomode/AGENTS.md`: Go Mode Frontend Integration
- `src/gomode/BrowserShell.tsx`: Browser-side Go Mode shell capabilities for the hosted caic frontend.
- `src/gomode/HostMode.test.ts`: Tests for Go Mode host-mode detection.
- `src/gomode/HostMode.tsx`: Go Mode host-mode context: exposes native bridge capabilities, including voice state, to the hosted frontend.
- `src/gomode/McpClient.ts`: MCP JSON-RPC client for listing backend tool descriptors and dispatching tool calls via the MCP endpoint.
- `src/gomode/VoiceGatewayProtocol.test.ts`: Golden tests for generated voice gateway protocol DTO JSON.
- `src/gomode/VoiceOverlay.module.css`: Styles for the VoiceOverlay component: full-width bottom panel with mic button and voice controls.
- `src/gomode/VoiceOverlay.test.tsx`: Tests for VoiceOverlay voice session connection.
- `src/gomode/VoiceOverlay.tsx`: Voice overlay component: persistent bottom panel with mic button and voice controls.
- `src/gomode/VoiceSession.test.ts`: Tests for the browser voice gateway session manager.
- `src/gomode/VoiceSession.ts`: Core voice gateway session manager for the web frontend via WebRTC.
- `src/gomode/VoiceState.ts`: Shared voice session state — set by VoiceOverlay, read by task list/cards.
- `src/gomode/notifications.ts`: Browser notification helpers for alerting when agents need attention.
- `src/grouping.test.ts`: Tests for groupMessages and groupTurns logic.
- `src/grouping.ts`: Pure grouping and turn-splitting logic for agent event streams.
- `src/images.ts`: Helpers for converting image files to base64 ImageData payloads.
- `src/index.tsx`: Application entry point — mounts the router and top-level providers.
- `src/pages/DiffPage.tsx`: DiffPage is the /task/:taskId/diff route for a task's file changes.
- `src/pages/EmptyPage.tsx`: EmptyPage is the index route for "/" with no detail content beside the sidebar.
- `src/pages/LoginPage.tsx`: LoginPage shows OAuth provider buttons when auth is enabled and user is not logged in.
- `src/pages/ProcessesPage.tsx`: ProcessesPage is the /task/:taskId/processes route for a task's process tree.
- `src/pages/SettingsPage.tsx`: SettingsPage is the /settings route, wiring application state into the settings form.
- `src/pages/TaskDetailPage.tsx`: TaskDetailPage is the /task/:taskId route for the live agent output stream.
- `src/pages/TaskInfoPage.tsx`: TaskInfoPage is the /task/:taskId/info route for recorded and observed task metadata.
- `src/pages/VncPage.tsx`: VncPage is the /task/:taskId/vnc route with the lazily-loaded noVNC desktop viewer.
- `src/quota.test.ts`: Tests for task quota countdown helpers.
- `src/quota.ts`: Shared quota helpers for task-card countdowns and recovery notifications.
- `src/routes.tsx`: Route tree shared by the app entry point and tests: nested layout routes under the shell.
- `src/sw.test.ts`: Tests that the service worker caches only immutable frontend assets.
- `src/taskEventTimeline.test.ts`: Focused tests for task SSE replay buffering, native retry ownership, and cleanup.
- `src/taskEventTimeline.ts`: SolidJS task event timeline primitive: owns SSE lifecycle, replay buffering, and live publication.
- `src/taskListEventValidator.test.ts`: Guard for the TaskListEvent SDK validator: proves the settled-history status
- `src/taskPath.test.ts`: Tests for task URL path helpers.
- `src/taskPath.ts`: URL path helpers for task routes: slug building and ID extraction.
- `src/test-setup.ts`: Global test setup for vitest with SolidJS testing-library.
<!-- END FILE INDEX -->
