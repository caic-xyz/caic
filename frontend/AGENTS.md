# SolidJS Web Frontend

SolidJS web UI for caic. Built with Vite, outputs to `../backend/frontend/dist/`.

## Linting

**Always** run `make lint-frontend` after any frontend file change. Do not skip this step.
**Always** run `make build` after any frontend file change. Do not skip this step.

## Stack

- SolidJS for reactivity
- Vite for bundling
- TypeScript with strict mode
- ESLint with solid plugin
- Vitest + @solidjs/testing-library for unit tests

## Testing

Unit tests use Vitest with jsdom and `@solidjs/testing-library`. Test files
live next to their source as `*.test.tsx` or `*.test.ts`.

```bash
pnpm test           # single run
pnpm test:watch     # watch mode
pnpm test:coverage  # single run with coverage report
```

- Render components with `render(() => <Comp />)`.
- Prefer role-based queries (`getByRole`, `getByText`) over test IDs.
- Use `@testing-library/user-event` for interactions.
- `@testing-library/jest-dom` matchers are available globally.

### Query strategy

Use semantic queries whenever the element represents a user-visible interaction:
- **`getByRole("button", { name: "Send" })`** — buttons with stable labels
- **`getByLabelText(...)`** — form fields with associated labels
- **`getByText(...)`** — visible text content

Use `data-testid` + `getByTestId(...)` for **internal component contracts**
(repo chips, clone dialogs, SSE-connected UI) where the element identity must
survive copy changes, layout refactors, and style rewrites. Testids are an
explicit stability contract between tests and components — use them without
guilt for developer-facing test targets.

### Portal content

If a component uses `<Portal>`, query the portal-rendered content with the
`screen` export instead of destructuring from `render()`:

```tsx
import { render, screen } from "@solidjs/testing-library";

render(() => <Comp />);
expect(screen.getByText("portal content")).toBeInTheDocument();
```

### Router tests

Components that use `useNavigate`, `useLocation`, or `<A>` need a router
context. Mock `@solidjs/router` with `vi.mock` to provide stubs:

```tsx
const navigateMock = vi.fn();
vi.mock("@solidjs/router", () => ({
  useNavigate: () => navigateMock,
  useLocation: () => ({ pathname: "/test-path" }),
  A: (props: Record<string, unknown>) => (
    <a href={props.href as string} onClick={(e) => { e.preventDefault(); navigateMock(props.href); }}>
      {props.children}
    </a>
  ),
}));
```

The `location` render option only works for actual `<Route>` components, not
for arbitrary components that consume router hooks. When testing a `<Route>`
component directly, use `render(() => <Route ... />, { location: "/path" })`
with `findBy*` queries (router is lazily loaded).

## Styling

CSS Modules with design tokens defined as CSS custom properties in `src/global.css`.
Use `var(--color-*)` and `var(--radius-*)` variables instead of hardcoded hex colors
or pixel radii. Add new tokens to `:root` in `global.css` when introducing new colors.

## Mobile

No back-navigation button is needed on mobile — the browser's swipe gesture
and back button handle it. Do not add a back arrow to the task detail header.

## Development

```bash
make frontend-dev   # Vite dev server on :5173, proxies /api to :2242
make dev            # Build + run Go server on :2242
```

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `public/sw.js`: Service worker for caic PWA.
- `src/App.module.css`: Styles for the App component.
- `src/App.test.tsx`: Tests for repo chip selection after clone and task creation.
- `src/App.tsx`: Application shell: top-level chrome (navbar, dialogs, voice, toasts) wrapping the routed panes.
- `src/AppState.tsx`: Application state store: owns task data, settings, SSE wiring, and task actions.
- `src/AuthContext.tsx`: Auth context: tracks the current user and auth configuration.
- `src/FunctionDeclarations.ts`: Gemini Live functions/tools for voice mode, sync with android/caic/src/main/java/com/fghbuild/caic/voice/FunctionDeclarations.kt
- `src/FunctionHandlers.ts`: Gemini function call dispatch for voice mode,
- `src/TaskNumberMap.ts`: Bidirectional task ID ↔ stable 1-based number mapping for voice mode,
- `src/VoiceSession.ts`: Core Gemini Live voice session manager for the web frontend via WebRTC. Keep in sync with android/caic/src/main/java/com/fghbuild/caic/voice/VoiceSession.kt
- `src/VoiceState.ts`: Shared voice session state — set by VoiceOverlay, read by task list/cards.
- `src/api.ts`: Singleton API client for the caic web UI.
- `src/components/AutoResizeTextarea.module.css`: Styles for the contenteditable AutoResizeTextarea component.
- `src/components/AutoResizeTextarea.test.tsx`: Tests for the AutoResizeTextarea component.
- `src/components/AutoResizeTextarea.tsx`: Auto-resizing editable div that starts as a single line and expands vertically.
- `src/components/Button.module.css`: Styles for the reusable Button component variants, icons, and loading state.
- `src/components/Button.tsx`: Reusable button component with variant and icon support.
- `src/components/CIDot.module.css`: CI status dot with tooltip showing check details.
- `src/components/CIDot.tsx`: Shared CI status dot used in TaskCard and TaskList repo headers.
- `src/components/CameraCapture.module.css`: Styles for the CameraCapture dialog.
- `src/components/CameraCapture.tsx`: Camera capture dialog: opens webcam, lets user take a photo, returns base64 ImageData.
- `src/components/CloneRepoDialog.module.css`: Styles for the CloneRepoDialog modal.
- `src/components/CloneRepoDialog.tsx`: Modal dialog for cloning a git repository by URL.
- `src/components/DiffDetail.module.css`: Styles for the full-page DiffView component.
- `src/components/DiffDetail.test.ts`: Tests for DiffDetail diff parsing utilities.
- `src/components/DiffDetail.tsx`: Full-page diff viewer for a task's file changes.
- `src/components/Dropdown.tsx`: Reusable dropdown menu with click-outside dismissal.
- `src/components/FormControls.module.css`: Shared form control component styles for selects and capability toggle chips.
- `src/components/FormControls.tsx`: Shared form control components for task harness selects and capability toggles.
- `src/components/Layout.module.css`: Shared layout component styles for route panes and detail-pane slots.
- `src/components/Layout.tsx`: Shared route layout wrappers for split panes and detail-pane slots.
- `src/components/MainLayout.module.css`: Styles for the new-task form in the main layout.
- `src/components/MainLayout.tsx`: Default layout: the new-task form, the sidebar task list, and the routed detail pane.
- `src/components/ProcessDetail.module.css`: Styles for the full-page ProcessDetail component with tree view.
- `src/components/ProcessDetail.test.tsx`: Tests for the process tree builder used by ProcessDetail.
- `src/components/ProcessDetail.tsx`: Full-page process tree viewer for a task's container.
- `src/components/ProgressPanel.module.css`: Styles for the ProgressPanel component.
- `src/components/ProgressPanel.tsx`: ProgressPanel renders active todos and subagent count from the agent event stream.
- `src/components/PromptInput.module.css`: Styles for the PromptInput component: container, drag-over, and image previews.
- `src/components/PromptInput.test.tsx`: Tests for the PromptInput component.
- `src/components/PromptInput.tsx`: Reusable prompt input with image support: paste, drag & drop, attach button, and preview strip.
- `src/components/RepoChipStrip.module.css`: Styles for the RepoChipStrip component.
- `src/components/RepoChipStrip.tsx`: Reusable repo chip strip with branch editing and add-repo dropdown.
- `src/components/SettingsForm.module.css`: Styles for the SettingsPage full-page settings view.
- `src/components/SettingsForm.tsx`: SettingsForm renders the application settings controls.
- `src/components/StatsIcon.module.css`: Styles for the StatsIcon component: 2×2 bar chart icon, popup panel, and perf table.
- `src/components/StatsIcon.tsx`: StatsIcon renders a 2×2 bar-chart icon in the task header that opens a popup
- `src/components/TaskCard.module.css`: Compact summary card for a single task.
- `src/components/TaskCard.tsx`: Compact card for a single task, used in the sidebar task list.
- `src/components/TaskDetail.module.css`: Styles for the TaskView component.
- `src/components/TaskDetail.test.tsx`: Tests for the TaskDetail diff link navigation and SSE connection behaviour.
- `src/components/TaskDetail.tsx`: TaskDetail renders the real-time agent output stream for a single task.
- `src/components/TaskList.module.css`: Sidebar task list panel.
- `src/components/TaskList.tsx`: Sidebar task list with collapsible panel, grouped by repo for active tasks.
- `src/components/Tooltip.module.css`: Tooltip shown on hover (desktop) and tap (mobile).
- `src/components/Tooltip.tsx`: Tooltip: hover on desktop, tap-to-toggle on mobile.
- `src/components/UsageBadges.module.css`: Usage badge styles — provider-grouped pills.
- `src/components/UsageBadges.test.tsx`: Tests for UsageBadges component.
- `src/components/UsageBadges.tsx`: Usage badges: per-provider grouped pills with color-coded thresholds.
- `src/components/VncViewer.module.css`: Styles for the VNC desktop viewer.
- `src/components/VncViewer.tsx`: noVNC-based desktop viewer for a task's container display.
- `src/components/VoiceOverlay.module.css`: Styles for the VoiceOverlay component: full-width bottom panel with mic button and voice controls.
- `src/components/VoiceOverlay.test.tsx`: Tests for VoiceOverlay voice session connection.
- `src/components/VoiceOverlay.tsx`: Voice overlay component: persistent bottom panel with mic button and voice controls.
- `src/components/WidgetCard.module.css`: WidgetCard: sandboxed iframe for agent-generated HTML widgets.
- `src/components/WidgetCard.tsx`: Sandboxed iframe widget card for agent-generated HTML widgets.
- `src/components/diffLines.ts`: Diff parsing and moved-block annotation for unified diffs.
- `src/effortOptions.ts`: Valid thinking-effort levels per harness, shared by the new-task form and fork dialog.
- `src/formatting.ts`: Shared formatting utilities, parallel to android/util/Formatting.kt.
- `src/global.css`: Global resets and design tokens. Keep color values in sync with android/caic/src/main/java/com/fghbuild/caic/ui/theme/Theme.kt
- `src/grouping.test.ts`: Tests for groupMessages and groupTurns logic.
- `src/grouping.ts`: Pure grouping and turn-splitting logic for agent event streams.
- `src/images.ts`: Helpers for converting image files to base64 ImageData payloads.
- `src/index.tsx`: Application entry point — mounts the router and top-level providers.
- `src/notifications.ts`: Browser notification helpers for alerting when agents need attention.
- `src/pages/DiffPage.tsx`: DiffPage is the /task/:taskId/diff route for a task's file changes.
- `src/pages/EmptyPage.tsx`: EmptyPage is the index route for "/" with no detail content beside the sidebar.
- `src/pages/LoginPage.tsx`: LoginPage shows OAuth provider buttons when auth is enabled and user is not logged in.
- `src/pages/ProcessesPage.tsx`: ProcessesPage is the /task/:taskId/processes route for a task's process tree.
- `src/pages/SettingsPage.tsx`: SettingsPage is the /settings route, wiring application state into the settings form.
- `src/pages/TaskDetailPage.tsx`: TaskDetailPage is the /task/:taskId route for the live agent output stream.
- `src/pages/VncPage.tsx`: VncPage is the /task/:taskId/vnc route with the lazily-loaded noVNC desktop viewer.
- `src/routes.tsx`: Route tree shared by the app entry point and tests: nested layout routes under the shell.
- `src/taskPath.ts`: URL path helpers for task routes: slug building and ID extraction.
- `src/test-setup.ts`: Global test setup for vitest with SolidJS testing-library.
<!-- END FILE INDEX -->
